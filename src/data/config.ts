const config = {
  defaultTitle: 'Caleb Chou',
  url: process.env.NODE_ENV !== 'development' ? process.env.CALEBCHOU : 'http://localhost:3040',
  defaultDescription: '',
  googleAnalyticsID: '',
};

export default config;
